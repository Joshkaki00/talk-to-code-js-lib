/**
 * TalkToCode - Natural Console Interface
 * Main export file
 */

import { talk, setContext, addToContext, enableVoiceMode } from './talkToCode.js';

export { talk, setContext, addToContext, enableVoiceMode };

export default {
  talk,
  setContext,
  addToContext,
  enableVoiceMode
};